# ft_package

A minimal Python package that provides a simple utility function:  
`count_in_list(lst, item)` — counts how many times a value appears in a list.

---

## Installation

After building the package, install it using one of the generated files:

```bash
pip install ./dist/ft_package-0.0.1.tar.gz
# or
pip install ./dist/ft_package-0.0.1-py3-none-any.whl
```
