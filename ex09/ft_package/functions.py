def count_in_list(lst, item):
    """
    Count how many times `item` appears in the list `lst`.
    Parameters:
        lst (list): The list to search in.
        item (any): The value to count.
    Returns:
        int: Number of occurrences of `item` in `lst`.
    """
    return lst.count(item)
